package easytier

const (
	// CLICommand 为调用 EasyTier 的 CLI 可执行文件名
	CLICommand = "easytier-cli"
	// DefaultRPCPort 为 EasyTier RPC 默认端口
	DefaultRPCPort = "15888"
	// DefaultImageTag 为推荐的 EasyTier 镜像 tag
	DefaultImageTag = "v2.2.3"
	// GitHubReleasesAPI 用于检查最新版本
	GitHubReleasesAPI = "https://api.github.com/repos/EasyTier/EasyTier/releases/latest"
)
