import type { SelectOption } from 'naive-ui'

export const CLI_RAW_TARGET_OPTIONS: SelectOption[] = [
  { label: 'peer（对等节点）', value: 'peer' },
  { label: 'route（路由）', value: 'route' },
  { label: 'node（本机）', value: 'node' },
  { label: 'version（版本）', value: 'version' },
]
